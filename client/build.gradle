apply plugin:'application'
mainClassName = "ru.vkirilchuk.examples.spring.hessian.client.Main"

dependencies {
    compile project(':api')
    compile "org.springframework:spring-web:${springVersion}"
    compile "com.caucho:hessian:${hessianVersion}"
}
